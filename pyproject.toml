[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "twgbg"
version = "0.1.0"
description = "Two-Way Global Bisimulation Game"
authors = [{name = "AI Engineer"}]
requires-python = ">=3.10"
dependencies = ["PyQt5>=5.15", "numpy>=1.26"]
