[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "twgbg"
version = "0.1.0"
description = "Two-Way Global Bisimulation Game"
authors = [{name = "AI Engineer"}]
requires-python = ">=3.10"
dependencies = ["PySide6>=6.6", "numpy>=1.26"]
